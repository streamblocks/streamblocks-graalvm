namespace test:
	actor test() ==> :
		action ==>
		do
			g := lambda (x) : lambda (y) : x + y end end;
            a := 11;
            f := g (a * a);
            a := 7;
            b := f(1);
            println(b);
		end
	end
end
